version: '3.8'

services:
  redis:
    image: redis/redis-stack-server:latest
    container_name: golf-rental
    ports:
      - "0.0.0.0:6379:6379"
    volumes:
      - redis-data:/data

volumes:
  redis-data: